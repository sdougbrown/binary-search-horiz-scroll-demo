import React, { useRef } from 'react';
import { galleryState } from '../../state';
import { log } from '../../utils/log';

import './style.scss';

// log(galleryState.state);

export function GalleryItem() {
}

function Gallery() {
  const el = useRef(null);

  return (
    <>

    </>
  )
}

export default Gallery

